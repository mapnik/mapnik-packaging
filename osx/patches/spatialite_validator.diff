--- src/shapefiles/validator.c	2014-03-20 12:10:18.000000000 -0700
+++ src/shapefiles/validator.c_	2014-03-20 12:10:15.000000000 -0700
@@ -3510,7 +3510,7 @@
 
 /* silencing stupid compiler warnings */
     if (sqlite == NULL || table == NULL || geom == NULL ||
-	||report_path == NULL || n_rows == NULL || n_invalids == NULL)
+	report_path == NULL || n_rows == NULL || n_invalids == NULL)
 	table = NULL;
 
     if (err_msg == NULL)
